# Public URL of the deployed Express API. Do not include /api or a trailing slash.
VITE_API_URL=https://api-v2.montessorischools.in

# Local development only: Vite proxies /api/* here when VITE_API_URL is not used.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:4000
